#!/bin/bash
if [ -n "$SNAP" ]; then
    export NO_AT_BRIDGE=1
    exec python3 "$SNAP/usr/share/safebox/safebox_gui.py" "$@"
else
    export NO_AT_BRIDGE=1
    exec python3 "/usr/share/safebox/safebox_gui.py" "$@"
fi
